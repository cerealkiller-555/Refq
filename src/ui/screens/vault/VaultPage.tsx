// ============================================================
// رِفق — Screen: المعرفة (Vault)
// ============================================================

export function VaultPage() {
  return (
    <section className="screen">
      <h2 className="screen-title">🧠 المعرفة</h2>
      <div className="placeholder-card">
        <p>مكتبة المعرفة (Markdown Vault) — تُبنى في P4.</p>
      </div>
    </section>
  );
}